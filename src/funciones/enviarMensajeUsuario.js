/*import conn from "../conexion.js";

function enviarMensajeUsuario(){
    const query = `SELECT emp_cl12 FROM lpb_cl12 WHERE emp_cl12 = 'Antartida' ORDER BY fum_cl12 DESC LIMIT 10`

    conn.query()

}

function ejecutarPorTiempo(func, tiempoEjecucion, intervalo) { 
    // Ejecutar la función inmediatamente 
    func(); 
    // Configurar un intervalo para llamar a la función repetidamente 
    const intervaloID = setInterval(func, intervalo); 

    // Configurar un temporizador para detener el intervalo después de un cierto tiempo 
    setTimeout(() => { 
        clearInterval(intervaloID); 
        console.log('Ejecución detenida');
    }, tiempoEjecucion); 
}*/

